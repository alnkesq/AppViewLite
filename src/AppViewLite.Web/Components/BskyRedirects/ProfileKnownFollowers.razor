@page "/profile/{did}/known-followers"
@inject NavigationManager Navigation

@code {
    [Parameter] public required string Did { get; set; }

    protected override void OnParametersSet()
    {
        Navigation.NavigateTo($"/@{Did}/known-followers");
    }
}
