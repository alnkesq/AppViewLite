@page "/"
@inject RequestContext RequestContext
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        if (RequestContext.IsLoggedIn) Navigation.NavigateTo("/following");
        else Navigation.NavigateTo("/@bsky.app/feed/whats-hot");
    }
}
